# AirPlay metadata is now handled by a Snapcast control script
# The control script is started automatically by snapserver when the stream starts
# See setup.sh for the controlscript configuration in the pipe source

# Old standalone metadata processor (disabled)
#[program:airplay-metadata]
#command=/usr/bin/python3 -u /app/scripts/process-airplay-metadata-v2.py
#user=snapcast
#directory=/app
#priority=50
#autostart=false
#autorestart=true
#startsecs=10
#startretries=3
#stdout_logfile=/var/log/supervisord/airplay-metadata.log
#stdout_logfile_maxbytes=10MB
#stderr_logfile=/var/log/supervisord/airplay-metadata_err.log
#stderr_logfile_maxbytes=10MB
#environment=PYTHONUNBUFFERED="1"